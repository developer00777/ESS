node_modules

# Output
.output
.vercel
.netlify
.wrangler
/.svelte-kit
/build

# OS
.DS_Store
Thumbs.db

# Env
.env
.env.*
!.env.example
!.env.test

# Vite
vite.config.js.timestamp-*
vite.config.ts.timestamp-*

# Local-only compose override (host port remaps for this machine)
docker-compose.local.yml

# Local Claude Code settings (machine-specific)
.claude/settings.local.json

# Contains real employee personal data — never publish
personal master data.xlsx
Designation master data*.xlsx
HR Team Master Tracker.xlsx

# Live integration credentials — the handover doc references this file rather
# than embedding the tokens, so they never enter git history.
docs/integration-credentials.md
